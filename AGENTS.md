# 项目规则

本项目继承 BenYan Engineering Standard v1.1.0、Fast Track 和共享 Web 前端规范。通用工程规则不在本文件重复。

## 项目目标

这是一个面向真实授课工作的课程素材库。

V1 产品定义以 `docs/product-v1.md` 为唯一依据。实现前先阅读该文件，不根据页面便利或技术习惯自行改变产品语义。

当前阶段目标是完成：

**录入 → 备课 → 使用 → 复盘 → 积累**

这一完整闭环。

## 工程状态和任务上下文

开始任何开发任务前，必须先阅读：

1. `AGENTS.md` —— 工程规则与执行约束；
2. `PROGRESS.md` —— 当前项目状态、已完成范围、当前基线与未完成事项；
3. 当前 Task 文件 —— 本次任务目标、边界与验收标准；
4. Task 明确引用的产品、架构或其他文档。

不要仅根据聊天记录、Git 历史或现有代码自行推断当前任务状态；当它们与 `PROGRESS.md` 或当前 Task 存在冲突时，先停止施工并指出冲突。

### PROGRESS.md

`PROGRESS.md` 是项目当前状态快照，不是历史日志。

它用于回答：

* 当前完成到哪个 Task；
* 当前已验证的基线是什么；
* 哪些产品或工程判断已经冻结；
* 当前系统已经具备什么；
* 哪些能力尚未实现；
* 当前正在执行什么 Task；
* 下一步是什么。

完成一个 Task 并通过独立验收后，应更新 `PROGRESS.md`，使下一位 Agent 无需从 Git 历史或聊天记录重新推断项目状态。

历史变化由 Git 保存，不在 `PROGRESS.md` 中维护流水账。


## 当前技术路线

沿用 Starter 默认技术栈，不做无理由替换：

* Python 3.12+
* FastAPI
* SQLAlchemy 2.x
* PostgreSQL
* Alembic
* React + TypeScript + Vite
* BenYan AI Design System
* pytest / Vitest / Playwright

前端服务端数据存在真实的查询、mutation 和缓存失效需求时，可以使用 TanStack Query。

## 产品约束

以下语义不得在实现过程中自行简化或改写：

* 素材、客户、课程、场次、使用记录是独立业务实体。
* 场次的人群类型允许多选。
* 使用记录的状态为：计划 / 已用 / 未用。
* 使用效果为：未评 / 好 / 差；`未评`不能替换成“默认一般”。
* 同源素材属于同一素材家族，重复使用检查按家族判断。
* 同客户重复为强提醒，同集团重复为弱提醒；都不能阻止用户继续使用。
* 系统提示不得自动修改素材人工状态。
* 历史效果不得凭推测自动补齐。
* 快速录入只强制标题、类型、正文。
* V1 的 Markdown 批量导入只负责拆分并生成草稿，不内置 AI 整理。
* 课后登记必须考虑手机使用，并以约 5 分钟能够完成一场课登记为体验目标。

如果实现需要改变以上语义，先修改产品定义并经过确认，不在代码中静默决定。

## 工程边界

保持架构简单，由真实问题推动复杂度。

不要为了目录完整创建没有业务意义的 Service、Repository、Manager、Utils 或抽象基类。

数据库 Schema 变更必须通过 Alembic。

前端继续使用相对路径 `/api/v1/*`，不得在业务代码中写死后端地址。

UI 必须复用 `ui/design-system/` 的 tokens、组件和规则，不另造品牌体系。

## 数据与公开仓库

本仓库公开，但真实业务数据不公开。

不得提交：

* 真实客户或学员数据
* 真实场次数据
* 内部来源备注
* 未公开课件内容
* Secret、Token、密码、生产连接信息
* 含真实业务数据的测试 fixture、截图或日志

示例和测试数据使用虚构或匿名化内容。

## V1 边界

除非产品定义明确变更，不提前加入：

* AI 推荐
* 向量数据库
* 多用户协作和复杂权限
* 智能相似度检索
* 自动素材画像
* 内容版本历史
* 复杂课程层级体系

不要为未来可能发生的需求提前设计复杂基础设施。

## 完成标准

每个任务完成后至少检查：

* 实现是否符合 `docs/product-v1.md`
* 正常、边界和失败场景是否有测试
* 是否引入不必要抽象或依赖
* 是否泄露真实业务数据或 Secret

提交前运行：

```bash
make check
```

涉及后端运行边界时运行：

```bash
make smoke
```

涉及页面或完整用户流程时运行：

```bash
make e2e
```

报告实际验证证据。

**AI self-check 不等于独立验收。**
