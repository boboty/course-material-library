Pill-shaped topic / filter chip. Lower emphasis than Badge — use for keyword lists and filters.

```jsx
<Tag>大模型</Tag>
<Tag active>研发提效</Tag>
<Tag onRemove={() => {}}>工作坊</Tag>
```

`active` gives the brand-tinted selected look; `onRemove` adds a × affordance.
